# Claude API Configuration
ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here

# Embedding Service Configuration (NO API KEY NEEDED!)
# Options: jina (default, recommended), huggingface
# Both are free and don't require API keys
EMBEDDING_SERVICE=jina

# Database Configuration
DATABASE_URL=postgresql://user:pass@localhost:5432/articledb

# Vector Database
QDRANT_URL=http://localhost:6333
# Vector size will be auto-configured based on embedding service:
# - JINA: 768 dimensions
# - HuggingFace: 384 dimensions

# Cache (Optional but recommended)
REDIS_URL=redis://localhost:6379

# Server Configuration
PORT=8080
HOST=0.0.0.0

# Environment
GO_ENV=development
NODE_ENV=development

# Rate Limiting
CLAUDE_RATE_LIMIT=1000

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your-api-key-here' with your actual Anthropic API key
# 3. Your API key should start with 'sk-ant-api03-'
# 4. Choose embedding service (jina or huggingface) - no API key needed!